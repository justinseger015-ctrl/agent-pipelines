# Work Stage - TDD implementation loop
# Runs N iterations, fresh agent each time

name: work
description: TDD implementation loop - implement tasks from beads

termination:
  type: fixed

delay: 3

# Commands available to the agent (passed via context.json)
# Semantics (required/optional) defined in prompt, not here
commands:
  test: ./scripts/tests/run_tests.sh
  # Projects can override/extend these in their stage config:
  # format: prettier --check .
  # types: tsc --noEmit
  # lint: eslint .
  # scan: ./scripts/scan.sh
